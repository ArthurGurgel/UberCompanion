# Copie este arquivo para `.env` e preencha as credenciais
# NÃO comite o arquivo `.env` com credenciais reais (está no .gitignore)

# Banco de dados
DB_HOST=localhost
DB_USER=seu_usuario
DB_PASSWORD=sua_senha
DB_NAME=seu_banco
DB_PORT=3306

# Chave secreta do Flask (mude para produção)
SECRET_KEY=troque-por-uma-chave-secreta
